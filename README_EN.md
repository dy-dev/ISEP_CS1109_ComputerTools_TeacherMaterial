# CS.1109 - Runner

Starting point for session 2. Open this folder in CLion (File > Open, pick
the folder that contains CMakeLists.txt), then Build and Run.

The program does nothing yet: it is a skeleton to complete by following the
lab instructions. The CMakeLists.txt file is what CLion uses to build the
project; it will be explained in session 4.
